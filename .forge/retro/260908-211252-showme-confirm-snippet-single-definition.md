# 2026-09-08 — 확정 버튼 스니펫 정본 1곳 + fg-doctor B19 (회고 학습의 eval 승급 사례)

## 계획 대비 실제
- 계획대로 된 것: 두 슬라이스 착지, DoD 5/5. `VISUAL.md` 사본 2→1(최소 예제는 "정본을 붙이라 + 두 번 안 쓰는 이유 + B19가 잡는다" 주석으로), fg-doctor B19를 sh·js 트윈에 같은 위치·같은 문구로 신설(3분기: missing / duplicated(N copies) / incomplete), behavior 84→94·parity 25→29. 뮤테이션(js에서 B19 제거)으로 parity FAILED 3·behavior FAILED 8을 확인해 가드가 fail-open이 아님을 증명, 복원 체크섬 일치. 이것은 회고 `260805-063357` 학습 3(사본 2곳 중 하나만 고쳐진 replace_all)이 ADR `260906-171420`의 eval로 승급된 첫 forge-내부 사례다.
- 발산: 없음(계획 그대로). 현장 결정 하나 — **incomplete 분기는 파일 전체가 아니라 정본 코드블록만 본다.** 산문 불릿이 `dataset.sent`를 언급하므로 파일 전체 grep은 가드 없는 스니펫도 통과시켰을 것이다(PLAN-FORMAT (e): 대리 지표 금지). 코드펜스 경계를 sh(line 산술)·js(배열 탐색)로 같은 결과가 나오게 구현했고 parity가 이를 고정한다.

## 학습
- 다음에 다르게 할 것:
  1. **새 기계 검사는 "구현을 빼면 실패하는가"까지 보여야 한다(뮤테이션).** 이 세션에서 두 번 값을 냈다 — A9는 픽스처 없이 세 게이트를 전부 통과하며 3 인터벌간 숨어 있었고(뮤테이션이 있었으면 첫날 드러났다), B19는 뮤테이션으로 가드 실재를 증명했다. PLAN-FORMAT runnable-DoD 규칙 (f)로 올릴 후보이지만 **표본이 한 세션의 2건**이라 이번엔 후속 후보로만 남긴다(사용자 확인). 다음에 또 값을 내면 fg-quick으로 한 줄 승급.
  2. **문서 안의 코드 스니펫을 검사할 땐 산문과 코드를 분리한다.** 같은 토큰이 설명 불릿에도 등장하므로 파일 grep은 항상 fail-open 쪽으로 틀린다. 코드펜스 추출이 필요하고, 두 트윈이 같은 경계를 잡는지는 parity 픽스처(good/dup/noprefix/missing)로 고정해야 한다.
  3. **문서에서 사본을 줄일 땐 "어디에 오는가"는 남긴다.** 최소 예제에서 버튼을 통째로 지우면 예제가 불완전해진다 — 위치 주석으로 대체하면 복붙하는 사람이 구멍 있는 버튼을 만드는 원 문제는 사라지고 예제의 완결성은 유지된다.

## 문서 갱신
- CONTEXT.md 승급: 없음.
- ADR 추가: 없음 — ADR `260805-005436`의 인라인 결정을 **유지**했다(vendored 5파일 무변경).
- Eval: **B19 자체가 이 task의 산출물**(`scripts/forge-doctor.{sh,js}` + behavior 5·parity 4 픽스처, `skills/fg-doctor/SKILL.md` 열거).

## 후속 작업 후보
- PLAN-FORMAT runnable-DoD (f) "뮤테이션 확인" — 다음 표본이 나오면 fg-quick.
- `ask-input` 전송 버튼의 재전송 가드(회고 `260805-063357` 학습 1) — 여전히 미착수, 이번 비목표.
