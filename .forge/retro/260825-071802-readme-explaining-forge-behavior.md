# 2026-08-25 — README 이중언어 본문에 Explaining forge 행동 서술 추가 (#127)

## 계획 대비 실제

- 계획대로. divergence **0** — 슬라이스 3개가 그대로 landed하고 Non-goal 7항목 전부 준수, DoD 7개의 사후 값이 계획 예측과 정확히 일치했다(`0→1`, `1→2`, `+2/-0` 대칭).

## 학습

- **다음에 다르게 — 여러 파일의 같은 자리를 편집할 땐 삽입 지점을 행 번호가 아니라 구조로 지정한다.** 계획이 "코드펜스 닫힘 뒤, `docs/state-contract.md` 안내줄 앞"으로 못 박았고 실행 스크립트가 그 구조를 `assert`로 검증했다. 두 README의 펜스 범위가 실제로 달라(EN `133~138` · KO `133~137`) **행 번호를 하드코딩했으면 한글판이 어긋났다.** 이중언어 쌍처럼 "같아 보이지만 미세하게 다른" 파일에서 특히 그렇다.

## 문서 갱신

- CONTEXT.md 승급: 없음
- ADR 추가: 없음 (3조건 미달 — 되돌리기 쉽고, 의아할 것이 없고, 진짜 트레이드오프가 없다)

> 이 파일을 쓴 이유: divergence 0이라 원래 "파일 없이 종결"로 판단했으나, 위 기법 한 줄이 아카이브된 `run.md`에만 남으면 **어떤 단계도 그것을 읽지 않는다**(fg-ask·fg-run은 `.forge/retro/`만 읽는다). 같은 회고에서 발견한 waiver 전제 붕괴가 즉시 자기를 실증한 사례라, 최소 회고로 남긴다.
