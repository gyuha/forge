# Codex interaction adapter

Use the user-input facility exposed by the current Codex mode. If structured
input is unavailable, render a numbered list and ask for one stable identifier.
Never invent a tool name or widen a confirmation gate.
