# Interaction contract

Ask one question at a time. Prefer the active host's structured-choice tool
when it exists and the option count fits. Otherwise print a numbered list and
ask the user for one stable identifier. Confirmation gates and defaults remain
identical across hosts.
